<div class="banner" style="background-color: <%= @colors.sample %>">
  <div class="container banner-content">
    <h1>MOSHI BLOG</h1>
    <p>Where interesting minds come together</p>
    <% if user_signed_in? %>
      <%= link_to "Write something amazing", new_article_path, class: "btn-sharp" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "btn-sharp" %>
    <% end %>
  </div>
</div>
